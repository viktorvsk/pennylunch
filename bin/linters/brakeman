#!/usr/bin/env ruby
# frozen_string_literal: true

require "rubygems"
require "bundler/setup"

ARGV.unshift("--no-pager", "--quiet", "--exit-on-warn", "--exit-on-error")

load Gem.bin_path("brakeman", "brakeman")
