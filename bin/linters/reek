#!/usr/bin/env ruby
# frozen_string_literal: true

require "rubygems"
require "bundler/setup"

ARGV.push("app", "lib") unless ARGV.any? { |argument| !argument.start_with?("-") }
ARGV.unshift("--config", File.expand_path("../../.reek.yml", __dir__))

load Gem.bin_path("reek", "reek")
