<% content_for :title, recipe.title %>

<%= render "show_toolbar" %>

<main class="recipe-page">
  <%= render "show_title", recipe: %>
  <%= render "show_detail", recipe: %>
  <%= render "similar_recipes", similar_recipes: %>
</main>
