<%= turbo_frame_tag "recipe-results-frame", data: { turbo_action: "advance" } do %>
  <main class="recipe-page">
    <% if selected_category.present? %>
      <section class="recipe-title-section">
        <h1 class="recipe-page-title"><%= category_heading(selected_category) %></h1>
      </section>
    <% end %>

    <% if recipes.any? %>
      <%= render "results_grid", recipes:, next_page_url:, selected_ingredient_names: recipe_filter_ingredient_names(filters["ingredients"]) %>
    <% else %>
      <%= render "empty_results" %>
    <% end %>
  </main>
<% end %>
